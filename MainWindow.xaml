<Window x:Class="WinStats.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinStats" Height="40" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="6" CaptionHeight="0" GlassFrameThickness="0" CornerRadius="6"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="10,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="6" Background="#CC111111" BorderBrush="#22FFFFFF" BorderThickness="1" 
            MouseLeftButtonDown="Border_MouseLeftButtonDown">

        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem x:Name="PinMenu" Header="Pin Overlay" Click="MenuPin_Click"/>
                <MenuItem Header="Hide Overlay" Click="MenuHide_Click"/>
            </ContextMenu>
        </Border.ContextMenu>

        <Viewbox Margin="4,2,4,2" Stretch="Uniform">
            <StackPanel x:Name="MainStackPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="CpuText" Text="CPU  --%   --°C" />
                <TextBlock x:Name="GpuText" Text="GPU  --%" />
                <TextBlock x:Name="RamText" Text="RAM  --%" />
            </StackPanel>
        </Viewbox>
    </Border>
</Window>